#include <stdio.h>

int main() {
    int size;
    printf("enter the number of element to store:\n");
    scanf("%d",&size);
    int a[size];
    for(int i=0;i<=size-1;i++)
    {
        printf("enter the %d value\n",i+1);
        scanf("%d",&a[i]);
    }
    printf("-----------\n");
    for(int i=0;i<=size-1;i++)
    {
        printf("%d",a[i]);
    }
    printf("0");
}