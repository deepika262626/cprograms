#include <stdio.h>

int main()
{
    int size;
    printf("enter the number of element to store:\n");
    scanf("%d",&size);
    int a[size];
    for(int i=0;i<=size-1;i++)
    {
        printf("enetr the %d value\n",i++);
        scanf("%d",&a[i]);
    }
    for(int i=1;i<=size-1;i++)
    {
        int min=a[0];
        if(a[i]<min)
        {
            min=a[i];
        }
    }
    printf("%d",main);
}