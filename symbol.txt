#include <stdio.h>
void main()
{
    char ch;
    printf("enter character value \n");
    scanf("%c",&ch);
    int res=(ch!='A')&&(ch!='Z')||(ch!='a')&&(ch!='z')||(ch!='0')&&(ch!='9');
    printf("%c is  not a symbol i.e is:%d",ch,res);
}